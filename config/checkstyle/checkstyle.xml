<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  BDEC 务实 checkstyle 规则集(里程碑 Phase 4).

  派生自 Sun/Google 约定,但刻意关闭两类"陷阱"检查:
    - MagicNumber:反编译器目标域里数值字面量遍布,无 lint 价值.
    - LineLength(100):JDK 链式调用与中文注释常超宽,留待后续手调.

  定位:`mvn verify` 时报告(severity=warning + failOnViolation=false),
  不阻断构建,随里程碑逐步清理违规.只保留高信号、低噪声的检查:
  导入卫生(本项目 Phase 2/3 重点清理项)、命名、空白与基础结构.
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <!-- 空白卫生 -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>
    <module name="NewlineAtEndOfFile"/>
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="message" value="行尾存在空白字符"/>
    </module>

    <module name="TreeWalker">
        <!-- 导入卫生:未用/重复/非法导入是真实技术债.
             (不启用 AvoidStarImport:发射器/翻译器对 ast.expr 等内部包
              刻意用通配导入,显式展开会引入 20+ 行噪音) -->
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>
        <module name="IllegalImport"/>

        <!-- 命名:仅类型/常量/包名,避免对成员名过严 -->
        <module name="TypeName"/>
        <module name="ConstantName"/>
        <module name="PackageName"/>

        <!-- 基础结构 -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>

        <!-- 括号(不启用 WhitespaceAfter:代码库统一采用紧凑 for 循环
             `for(i=0;i<n;i++)`,分号后无空格属刻意风格) -->
        <module name="ParenPad"/>
        <module name="TypecastParenPad"/>
    </module>
</module>
