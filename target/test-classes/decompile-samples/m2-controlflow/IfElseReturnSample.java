package test;

/** M2: if-else with returns in both branches. */
public class IfElseReturnSample {
    public int test(boolean flag) {
        if (flag) {
            return 1;
        } else {
            return 0;
        }
    }
}
