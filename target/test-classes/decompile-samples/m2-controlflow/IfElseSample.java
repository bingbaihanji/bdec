package test;

/** M2 control flow test: if-else branching. */
public class IfElseSample {
    public int test(boolean flag) {
        if (flag) {
            return 1;
        } else {
            return 0;
        }
    }
}
